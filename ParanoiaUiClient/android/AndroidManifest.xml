<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.paranoia.client"
    android:installLocation="auto"
    android:versionCode="-- %%INSERT_VERSION_CODE%% --"
    android:versionName="-- %%INSERT_VERSION_NAME%% --">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- #30 in-app обновление: установка скачанного APK через системный диалог. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Управление audio-стеком во время звонка: переключение режима в
         MODE_IN_COMMUNICATION и speakerphone. Без этого permission'а
         setMode/setSpeakerphoneOn молча игнорируются и звук QAudioSink
         уходит в media stream, который при одновременном recording'е
         приглушается аудиосервисом. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Точные alarm'ы для polling-цикла сервиса уведомлений. Без exact-alarm'а
         app standby bucket (working set+) откладывает setAndAllowWhileIdle на
         минуты, и фоновые уведомления опаздывают. USE_EXACT_ALARM на Android 13+
         выдаётся системой автоматически как core feature для messaging-приложений;
         SCHEDULE_EXACT_ALARM — fallback для 12, который пользователь может
         подтвердить руками. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Wake lock — receiver alarm'а удерживает CPU на время сетевого poll'а,
         пока устройство в Doze. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- %%INSERT_FEATURES -->
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:hardwareAccelerated="true"
        android:label="-- %%INSERT_APP_NAME%% --"
        android:icon="@mipmap/icon"
        android:roundIcon="@mipmap/icon_round"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:fullBackupOnly="false">
        <!-- Единственная активити приложения — подкласс QtActivity (см.
             ParanoiaActivity.java, добивает зависший Qt-shutdown). И иконка
             лаунчера, и тап по уведомлению целятся прямо в неё (см.
             openAppIntent): Qt — singleton на процесс, любой activity-trampoline,
             роняющий QtActivity в отдельную task, приводит к зависанию.

             ⚠️ launchMode ОБЯЗАТЕЛЬНО singleTop, НЕ singleTask: Qt for Android
             официально не поддерживает singleTask/singleInstance — реюз task'и при
             живом/полумёртвом процессе даёт НЕУБИВАЕМЫЙ БЕЛЫЙ ЭКРАН на входе
             (проверено: пробовали singleTask ради share-into-live-process — вернул
             белый экран). share-warm (без PIN в живой процесс) надо решать иначе,
             не через launchMode. Второй QtActivity-инстанс добивает watchdog
             (ParanoiaActivity.onDestroy). -->
        <activity
            android:name="app.paranoia.client.ParanoiaActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:label="-- %%INSERT_APP_NAME%% --"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateAlwaysHidden|adjustNothing"
            android:screenOrientation="unspecified"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="-- %%INSERT_APP_LIB_NAME%% --" />

            <meta-data
                android:name="android.app.arguments"
                android:value="-- %%INSERT_APP_ARGUMENTS%% --" />
        </activity>

        <!-- Share-target живёт в ОТДЕЛЬНОЙ лёгкой не-Qt Activity (ShareActivity):
             ловит ACTION_SEND/SEND_MULTIPLE, кладёт payload в prefs и запускает
             launcher-intent (переиспользует живой ParanoiaActivity через onNewIntent,
             как тап по иконке). Так нет второго QtActivity-инстанса/нового процесса
             с залоченным vault'ом. taskAffinity="" + excludeFromRecents + noHistory +
             Translucent.NoTitleBar — трамплин не светится и не оставляет следа. -->
        <activity
            android:name="app.paranoia.client.ShareActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <service
            android:name="app.paranoia.client.ParanoiaForegroundService"
            android:enabled="true"
            android:exported="false"
            android:process=":notifications"
            android:foregroundServiceType="remoteMessaging|dataSync"
            android:stopWithTask="false" />

        <!-- Получатель alarm-будилки. Должен жить в том же процессе, что и
             сервис (:notifications), иначе тянет за собой инициализацию main
             процесса с Qt-биндингами. -->
        <receiver
            android:name="app.paranoia.client.ParanoiaForegroundService$PollAlarmReceiver"
            android:enabled="true"
            android:exported="false"
            android:process=":notifications" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/qtprovider_paths" />
        </provider>
    </application>
</manifest>
